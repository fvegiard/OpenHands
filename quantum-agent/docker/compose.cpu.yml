# Overlay for hosts without NVIDIA GPU.
# Usage: `docker compose -f docker-compose.yml -f compose.cpu.yml up`
services:
  quantum:
    runtime: runc
    environment:
      - NVIDIA_VISIBLE_DEVICES=
    image: quantum-agent:dev-cpu
    build:
      args:
        BASE: node:22-bookworm
